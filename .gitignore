# Secrets — never commit
.env

# Database
performance.db

# Cache (keep .gitkeep, ignore generated JSON)
cache/*.json

# MiniLM embedding model weights (downloaded on first run, ~80MB)
.st_cache/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/

# Rolling dedup state (cache-tier, evictable — not a permanent record)
seen_comment_urns.json
seen_reddit_threads.json
seen_reddit_leads.json

# Generated output
output/

# OS
.DS_Store
Thumbs.db
